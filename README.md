These files are staged for the public `neuralmux/installer` repository.
They are not used by the nmux build — they serve as the canonical source
for the public installer repo's contents.

- `install.sh` — Bash wrapper that downloads and execs the installer binary
- `release.yml` — GitHub Actions workflow to build and release the installer binary
